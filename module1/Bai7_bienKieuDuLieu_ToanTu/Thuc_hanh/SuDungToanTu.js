let inputWith;
let inoutHeight;

inputWith = prompt("Enter the width");
inoutHeight = prompt("Enter the height");

let width = parseInt(inputWith);
let height = parseInt(inoutHeight);

let area = width * height;

document.write('The area is : ' + area);